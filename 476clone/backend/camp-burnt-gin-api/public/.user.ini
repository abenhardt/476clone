; PHP upload limits for the Camp Burnt Gin API
; These override the system php.ini when using PHP-FPM or CGI.
; For php artisan serve, start the server with:
;   php -d upload_max_filesize=15M -d post_max_size=20M artisan serve

upload_max_filesize = 15M
post_max_size = 20M
memory_limit = 256M
